export interface Product {
    id: number;
    name: string;
    image: string;
    details?: string;
    description?: string;
    price?: string;
    originalPrice?: string;
    discountedPrice?: string;
}